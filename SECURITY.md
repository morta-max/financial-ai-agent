# 安全策略 (Security Policy)

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 1.x  | ✅ 活跃支持 |

## 报告安全漏洞

如果你发现了安全漏洞，**请不要公开提交 Issue**。

请通过以下方式私下报告：

- 📧 发送邮件至：**2898532734@qq.com**
- 主题格式：`[Security] 简要描述`
- 请包含：漏洞描述、复现步骤、影响范围、可能的修复建议

### 我们会：

- 🕐 **3 个工作日内**确认收到报告
- 📅 **10 个工作日内**提供初步评估
- 🔧 确认后尽快发布修复版本
- 🙏 在修复后公开感谢报告者（如果报告者同意）

## 安全最佳实践

### 部署时务必注意

1. **API 密钥保护**
   ```bash
   # 不要将 .env 提交到版本控制
   echo ".env" >> .gitignore
   # 使用环境变量注入，而非硬编码
   ```

2. **生产环境 CORS 配置**
   ```python
   # 将 ALLOWED_ORIGINS 设置为你的前端域名
   ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com
   ```

3. **HTTPS 强制**
   - 生产环境必须使用 HTTPS
   - 配置反向代理（Nginx/Caddy）终止 SSL

4. **速率限制**
   - 默认限制已启用（200 请求/分钟）
   - 根据实际负载调整 `middleware.py` 中的参数

5. **输入验证**
   - 所有 API 端点已强制验证输入
   - 不要禁用 `validators.py` 中的检查

6. **依赖安全**
   ```bash
   # 定期检查 Python 依赖漏洞
   pip-audit
   # 检查 npm 依赖
   npm audit
   ```

7. **Docker 安全**
   ```bash
   # 以非 root 用户运行（已在 Dockerfile 中配置）
   # 定期更新基础镜像
   docker pull python:3.11-slim
   docker pull node:20-alpine
   ```

### 已知风险

| 风险 | 严重性 | 缓解措施 |
|------|--------|---------|
| AKShare 数据可能有延迟 | 低 | 已在文档中标注 |
| 本工具不构成投资建议 | 中 | 免责声明已包含 |
| DuckDB 文件未加密 | 低 | 本地文件，建议磁盘加密 |

## 安全功能

本项目已实施以下安全措施：

- ✅ **XSS 防护**: 前端所有用户输入经过 HTML 转义
- ✅ **输入验证**: 股票代码、日期、搜索词严格校验
- ✅ **速率限制**: 令牌桶算法，分层限制
- ✅ **SQL 注入防护**: DuckDB 参数化查询
- ✅ **安全响应头**: X-Content-Type-Options, X-Frame-Options, CSP
- ✅ **CORS 白名单**: 生产环境限制允许域名
- ✅ **错误信息脱敏**: 不暴露内部异常详情
- ✅ **请求超时**: 前端 15s 超时 + AbortController
- ✅ **React 错误边界**: 组件崩溃不影响整体页面

## 致谢

感谢所有通过私下渠道报告安全问题的研究者。我们会在获得许可后在这里列出贡献者名单。
